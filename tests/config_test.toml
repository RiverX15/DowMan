[downloader]
# Network Settings
max_concurrent = 4
chunk_size_mb = 0.01    # 10 KB chunks
timeout_seconds = 0.15

[resilience]
max_retries = 3
exp_backoff_multiplier = 0.1
exp_backoff_base = 2
exp_backoff_min = 0.01
exp_backoff_max = 0.1
max_requeue_limit = 2
state_save_interval_chunks = 2